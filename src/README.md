# Carpeta reservada

El código de la aplicación todavía vive en `app/`, `components/`, `lib/` y `db/`. `src/` se creó para la estructura solicitada, pero no se trasladaron archivos: hacerlo ahora exigiría adaptar Vinext, las rutas, los alias de TypeScript y Drizzle. Consulta `Docs/arquitecture.md` antes de iniciar esa migración.
